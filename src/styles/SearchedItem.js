import styled from 'styled-components';

const SearchedItem = styled.View`
  background-color: ${props => props.theme.brown};
	padding: 5px 10px;
	margin: 10px 20px 10px 0px;
	position: relative;
`;
export default SearchedItem;